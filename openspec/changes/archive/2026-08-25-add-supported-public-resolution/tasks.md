## Tasks

All behaviour tasks run against the disposable-database harness (`test_support::TestDatabase`) or pure unit scope as noted. The gate command list lives in `DEVELOPMENT.md`.

- [x] 1.1 RED: In `crates/instagram-archive/tests/capability.rs`, add `public_resolution_reports_supported_after_its_plan_item_lands` asserting `AcquisitionMode::PublicResolution.capability()` has status `Supported`, wire method exactly `["public_resolution"]`, and ceiling `explicit_user_capture`; update the existing unimplemented-lanes test to cover only `OwnAccountSync`, `DataExport`, `LegacyImport`. Expected failure: status assertion reads `Planned`, not `Supported`.
- [x] 1.2 GREEN: Flip the `PublicResolution` arm in `crates/instagram-archive/src/capability.rs` to `SupportStatus::Supported`. Test 1.1 passes; full crate suite stays green.
- [x] 2.1 RED: Add `crates/instagram-archive/tests/resolution.rs` with fixture file `tests/fixtures/oembed/reel_public.json` and a hand-written `FakePublicSurface`; test `resolved_payload_is_stored_as_an_immutable_oembed_revision` submits a capture, resolves its permalink through a payload outcome, and asserts one `media_revisions` row exists whose raw evidence is kind `oembed_response`, whose bytes equal the fixture body byte for byte, whose content hash matches those bytes, and which carries parser version `instagram.oembed.v1`. Minimal compiling stubs (typed outcome enum, trait, `Database::resolve_capture_permalink` returning an error) are added so the failure is the missing-table/assertion failure this task states. Expected failure: the revision query fails because `media_revisions` does not exist yet.
- [x] 2.2 GREEN: Edit `schema.sql` in place — add `raw_records.body bytea not null`, set `blob_ref` comment/semantics to the hex SHA-256 content address, add `instagram_archive.media_revisions` with its foreign keys, add `media.current_revision_id` and widen `media_media_type_check` with `'unknown'` — and implement the insert path in the new `src/resolution.rs` module so test 2.1 passes.
- [x] 3.1 RED: In `tests/resolution.rs`, add `second_resolution_appends_a_revision_and_leaves_the_first_untouched`: resolve once on payload A, then again on changed payload B; assert two revisions exist in resolution order, every stored field of revision one equals what attempt one wrote (raw record id, hash, parser version, resolved time), and no row was mutated between attempts. Expected failure: second resolution currently overwrites or errors — assertion on two distinct revisions fails.
- [x] 3.2 GREEN: Implement re-resolution append semantics (new ids each attempt, media upsert repoints `current_revision_id`) so 3.1 passes.
- [x] 4.1 RED: Unit scope in `src/resolution.rs` tests: `normalizing_the_same_fixture_twice_is_identical` parses the committed reel fixture twice and asserts field-for-field equal normalized values; `unknown_payload_fields_stay_out_of_the_normalized_record` asserts normalization of the fixture equals normalization of the same fixture plus extra undocumented keys. Expected failure: functions do not exist / outputs differ.
- [x] 4.2 GREEN: Implement the pure normalization function with documented optional fields so both unit tests pass.
- [x] 5.1 RED: In `tests/resolution.rs`, add `resolved_media_carries_public_resolution_provenance_and_capture_linkage`: after resolving for an accepted capture, assert media acquisition method `public_resolution`, saved authority `explicit_user_capture`, upstream status `available`, capture links to that media id with status `resolved`, captured time and note unchanged; also `normalized_media_points_at_the_newest_revision` after two resolutions. Expected failures: linkage/provenance assertions fail.
- [x] 5.2 GREEN: Complete the transactional success path (observation `available`, capture update, provenance constants) so 5.1 passes.
- [x] 6.1 RED: Add `unsupported_outcome_fabricates_nothing`: surface answers `Unsupported`; assert observation kind `unsupported` appended, capture `unavailable`, zero media rows, zero revisions. Expected failure: unsupported classification not implemented.
- [x] 6.2 GREEN: Map the unsupported outcome verbatim so 6.1 passes.
- [x] 7.1 RED: Add `failure_kinds_survive_verbatim_without_inventing_deletion`: private, transient, and transport-failure outcomes in turn produce observations `private`, `temporarily_unavailable`, `resolution_failed`; none claims `deleted`; no media or revision exists; earlier successful revisions from a prior resolve stay intact. Expected failure: kind mapping incomplete.
- [x] 7.2 GREEN: Finish the outcome-to-observation mapping so 7.1 passes.
- [x] 8.1 Documentation task (no failing test possible: prose): update `docs/CAPABILITY_MATRIX.md` matrix row and status prose for the flipped lane, README status paragraph and milestone wording, DEVELOPMENT.md implemented-items sentence, and `lib.rs` module docs listing public resolution as implemented.
- [x] 8.2 Refactor after green: align naming and comments across `resolution.rs` and schema comments with the spec vocabulary; adds no test and changes no behaviour.
- [x] 9.1 Gate: run the full command list from `DEVELOPMENT.md` (fmt check, clippy `-D warnings`, deny, build, workspace tests, doc tests, release build) plus `git diff --check`, `openspec validate --all --strict`, `openspec validate --archived`; tick tasks only with evidence.
