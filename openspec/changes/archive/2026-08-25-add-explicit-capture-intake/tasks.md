## 1. Permalink canonicalization

- [x] 1.1 Add `crates/instagram-archive/tests/permalink.rs` with the canonicalization table tests against a skeleton `src/permalink.rs` whose `canonicalize` returns `unimplemented!`-free placeholder errors so failures land on assertions, not compile errors: every accepted form in the table (`http`/`https`, `instagram.com`, `www.`/`m.`/`l.` hosts, both `instagr.am` domains, `/p/`, `/reel/`, `/reels/`, `/tv/`, username-prefixed paths, tracking query and fragment carriers, with and without trailing slashes) yields its exact expected `https://www.instagram.com/{p|reel|tv}/{shortcode}/`; determinism repeats one input; mixed-case shortcodes survive byte for byte; refusals carry their class (malformed URL, foreign host, non-permalink path on a supported host, credentials/port authority, missing or over-long shortcode) and each refusal class is asserted by variant. Verification: `cargo test -p ratatoskr-instagram-archive --test permalink --locked` fails on the stated assertions.
- [x] 1.2 Implement `src/permalink.rs` until green: the typed reason enum, host/scheme/path parsing, alias and username-prefix normalization, query/fragment stripping, trailing-slash form, shortcode validation `[A-Za-z0-9_-]{1,64}` case-preserved; export through `lib.rs`. No code beyond what the tests require. Verification: `--test permalink` green.

## 2. Capability flip

- [x] 2.1 Add a test to `crates/instagram-archive/tests/capability.rs` asserting `ExplicitCapture` reports `Supported` while `PublicResolution`, `OwnAccountSync`, `DataExport`, `LegacyImport` do not — it must fail today because the matrix still reports `Planned` everywhere. Verification: the new assertion fails on the support-status comparison.
- [x] 2.2 Flip `AcquisitionMode::capability()` so `ExplicitCapture` reports `SupportStatus::Supported`, update the module documentation that says no mode is supported, move the matrix row in `docs/CAPABILITY_MATRIX.md` to `Supported`, and relax any now-stale prose in that document's header. Verification: `--test capability` fully green.

## 3. Capture identity in the schema

- [x] 3.1 Add schema tests to `crates/instagram-archive/tests/schema.rs`: inserting two rows into `instagram_archive.captures` with equal `(user_ref, canonical_url)` fails violating a uniqueness constraint named in the assertion, distinct pairs succeed, and the catalog shows a `client_idempotency_key` column on `captures`. These fail today: the constraint does not exist and neither does the column. Verification: the new assertions fail as stated.
- [x] 3.2 Edit `schema.sql` in place: add `client_idempotency_key text` (nullable) and `constraint captures_user_canonical_key unique (user_ref, canonical_url)` to `captures`, and rewrite the table comment that defers dedup keys to plan item 3. No other declaration changes. Verification: the new schema assertions pass and `--test schema` stays green.

## 4. Intake persistence and unavailable fallback

- [x] 4.1 Add `crates/instagram-archive/tests/capture.rs` against stub signatures that return not-implemented errors so failures are assertion-level: fresh submission creates a capture whose saved authority is `explicit_user_capture`, whose acquisition method follows the client source mapping, whose captured time equals the supplied instant, and whose stored client idempotency key round-trips; duplicate submission with different captured-at/note/client-source/key reports reuse of the original id and leaves one row with original values; different user and different URL produce distinct captures; racing duplicates converge on one row; recording unavailability kind `private` flips status to `unavailable`, appends an observation bound to the capture with no media binding, preserves url/timestamp/note byte-for-byte, creates no media row, accumulates on repeat, and refuses unknown capture ids with a not-found error. Verification: `cargo test -p ratatoskr-instagram-archive --test capture --locked` fails on the stated assertions.
- [x] 4.2 Implement `src/capture.rs` until green: request/record types, client-source-to-acquisition-method mapping, transactional submit (`ON CONFLICT DO NOTHING` then read), `record_capture_unavailable` appending to `availability_observations` and updating status only from `accepted`/`resolved`, typed error set; export through `lib.rs`. Verification: `--test capture` green.

## 5. Product HTTP intake surface

- [x] 5.1 Add `services/instagram-archive/tests/capture_api.rs` driving `product_router` through tower: grammar-shaped POST answers `201` with created capture fields and canonical URL; unchanged replay answers `200` marked reused; unknown platform, profile-path URL, client source `telegram`, unparsable body, and unparsable timestamp each answer `400` with a machine-readable code and no row persists behind any of them. These fail today: no product router exists. Verification: `cargo test -p ratatoskr-instagram-archive-service --test capture_api --locked` fails because `product_router` does not exist.
- [x] 5.2 Implement the surface: `ApiConfig { listen_address }` under `RATATOSKR__API__LISTEN_ADDRESS` (default `127.0.0.1:9083`, loopback-only) with config tests extended for the new key, `product_router(Database)` with the `/v1/captures` handler mapping domain outcomes to `201`/`200`/typed `400`s and never leaking driver errors, main.rs binding and draining the second listener like the first, and intake counters recorded at the handler boundary. Verification: `--test capture_api` and `--test boot` green.

## 6. Documentation alignment

- [x] 6.1 Update `README.md` status paragraph and capture-flow section, `DEVELOPMENT.md` status line, and the OpenSpec project context in `openspec/config.yaml`: explicit capture intake implemented (canonicalization, idempotent identity, unavailable fallback, product plane), public resolution and everything after still planned. This task cannot start from a failing test: documentation.
- [x] 6.2 Confirm every spec scenario added by this change names the test that executes it and no documented behavior contradicts README.md, DEVELOPMENT.md, or CAPABILITY_MATRIX.md; fix drift found. This task cannot start from a failing test: documentation consistency.

## 7. Full gate

- [x] 7.1 Archive this change (deltas fold into `openspec/specs/`), then run the complete gate from DEVELOPMENT.md on a clean tree — `git diff --check`, `openspec validate --all --strict`, `openspec validate --archived`, `cargo fetch --locked`, `cargo deny --locked check`, `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `cargo build --workspace --locked`, `cargo test --workspace --locked`, `cargo test --workspace --locked --doc`, `cargo build --workspace --locked --release` — and record the results. Verification: every command exits zero.
