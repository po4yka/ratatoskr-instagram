## 1. First-Version Lifecycle Schema and Complete Deletion Inventory

- [x] 1.1 RED: extend `crates/instagram-archive/tests/schema.rs` with `lifecycle_schema_exposes_policy_deletion_reresolution_and_reprocessing_state`; run it through `build-gate -- cargo test --locked -p ratatoskr-instagram-archive --test schema lifecycle_schema_exposes_policy_deletion_reresolution_and_reprocessing_state` and confirm the assertion fails naming the missing item-9 relations/columns/constraints, not a compile error.
- [x] 1.2 GREEN: edit the current `schema.sql` in place to add constrained media policy/reference state, deletion operation/effect and local-removal state, blob tasks, re-resolution runs/items, and export-reprocessing runs/items; replace the content-retaining tombstone path without adding migrations/tooling/version negotiation, then verify task 1.1 passes.
- [x] 1.3 RED: add `crates/instagram-archive/tests/privacy_deletion.rs::deletion_classifies_every_owned_data_and_blob_class`; compare the exact fresh-database table inventory plus protected BlobStore classes with capture and connection maps and confirm the assertion fails with the unclassified classes.
- [x] 1.4 GREEN: add the closed `OwnedDataClass` inventory and total capture/connection classifications with no duplicates or unknowns, then verify task 1.3 and the whole `privacy_deletion` test target pass.

## 2. Reference-Only Media Policy and Blob Convergence

- [x] 2.1 RED: add `crates/instagram-archive/tests/media_retention.rs::metadata_observation_never_fetches_without_an_authorized_policy`; use a recording transport and confirm the assertion fails because the current reference path has no explicit metadata-only decision.
- [x] 2.2 GREEN: implement the pure reference-only-by-default policy boundary and closed reasons, then verify task 2.1 passes with zero transport/store calls.
- [x] 2.3 RED: add `crates/instagram-archive/tests/media_retention.rs::archival_refuses_before_io_when_any_guard_is_unknown_or_exhausted`; table-drive provenance, explicit action, rights, HTTPS, type, size, lifetime, object and owner budget guards and confirm the expected metadata-only reasons are absent.
- [x] 2.4 GREEN: enforce every guard and issue an immutable finite archive lease only when all admit, then verify task 2.3 and all pure policy tests pass.
- [x] 2.5 RED: add `crates/instagram-archive/tests/media_retention.rs::verified_bytes_attach_only_after_url_mime_size_and_digest_checks`; confirm a redirect/type/size/digest mismatch currently lacks the asserted refusal and partial-storage cleanup.
- [x] 2.6 GREEN: implement bounded streaming through the approved HTTP seam and private content-addressed store, commit a typed `BlobRef` only after full verification, preserve user-upload provenance separately, and verify task 2.5 passes.
- [x] 2.7 RED: add `crates/instagram-archive/tests/media_retention.rs::expiring_one_reference_preserves_a_blob_referenced_elsewhere`; create two live references to one digest and confirm cleanup lacks the expected retained-shared result.
- [x] 2.8 GREEN: implement database-wide live-reference enumeration and exact digest/length-verified deletion scheduling without holding a connection during filesystem I/O, then verify task 2.7 passes.
- [x] 2.9 RED: add `crates/instagram-archive/tests/media_retention.rs::failed_blob_delete_stays_pending_until_verified_absent`; inject one filesystem failure and confirm the expected pending-then-complete transition is absent.
- [x] 2.10 GREEN: implement idempotent blob-task processing, safe bounded failure classes, absence verification, and overall operation convergence, then verify task 2.9 and the full media-retention target pass.

## 3. Capture and Account-Connection Privacy Deletion

- [x] 3.1 RED: add `crates/instagram-archive/tests/privacy_deletion.rs::cross_owner_or_unknown_target_refuses_without_mutation`; snapshot database, blob inventory, and outbox and confirm the expected owner-bound refusal behavior is unavailable.
- [x] 3.2 GREEN: implement validated capture/connection targets and stable owner-scoped operation identity, then verify task 3.1 passes.
- [x] 3.3 RED: add `crates/instagram-archive/tests/privacy_deletion.rs::preview_matches_apply_counts_for_unchanged_state`; confirm deterministic per-class preview/apply parity and preview's zero-mutation assertion fail before the planner exists.
- [x] 3.4 GREEN: implement the pure bounded deletion planner, read-only preview, target-locked recomputation, content-free report, and stale-plan refusal, then verify task 3.3 for both target kinds.
- [x] 3.5 RED: add `crates/instagram-archive/tests/privacy_deletion.rs::deleting_one_duplicate_capture_preserves_shared_source_and_emits_no_removal`; confirm the current archive cannot remove only capture-specific data while retaining shared media/revisions/raw evidence.
- [x] 3.6 GREEN: implement capture-specific deletion plus explicit same-owner/cross-owner/cross-lane retain and detach decisions, then verify task 3.5 passes.
- [x] 3.7 RED: add `crates/instagram-archive/tests/privacy_deletion.rs::deleting_final_capture_commits_complete_erasure_audit_blob_work_and_one_removal`; confirm required per-class removals, content-free audit, local guard, blob tasks, and `SocialSourceRemoved(UserRequested)` are absent.
- [x] 3.8 GREEN: implement final-source SQL cleanup, content-free audit/effects, resurrection guard, blob-task creation, and atomic canonical removal publication, then verify task 3.7 and assert the retained payloads contain no URL/caption/note/body/credential/private path.
- [x] 3.9 RED: add `crates/instagram-archive/tests/privacy_deletion.rs::connection_deletion_erases_credentials_and_exclusive_state_but_preserves_an_explicit_capture`; confirm complete connection cleanup and unchanged capture provenance do not both hold today.
- [x] 3.10 GREEN: implement connection-scoped credential/OAuth/capability/usage/sync/profile/raw/revision/media/link cleanup with shared holding retention, then verify task 3.9 passes without changing capture/export authority.
- [x] 3.11 RED: add `crates/instagram-archive/tests/privacy_deletion.rs::connection_only_sources_each_emit_one_removal_and_completed_replay_is_idempotent`; confirm the expected stable event set and single operation/effect audit are absent.
- [x] 3.12 GREEN: implement stable-order connection-only final-holding enumeration and completed-operation replay, then verify task 3.11 passes without duplicate SQL/blob/outbox effects.
- [x] 3.13 RED: add `crates/instagram-archive/tests/publishing.rs::late_knowledge_completion_cannot_resurrect_a_privacy_removed_source`; confirm a matching completion currently links instead of being skipped after physical capture deletion.
- [x] 3.14 GREEN: update completion intake and publication guards to use content-free local source removals rather than content-retaining capture tombstones, then verify task 3.13 and the full privacy-deletion/publishing targets pass.

## 4. Budgeted Recent-Capture Re-Resolution

- [x] 4.1 RED: add `crates/instagram-archive/tests/re_resolution_jobs.rs::selection_admits_only_due_recent_live_transient_or_resolved_captures`; include resolved, transient, private, deleted, unsupported, permanent-unavailable, old, not-due, and locally removed fixtures and confirm eligible/skipped ordering differs from the expected set.
- [x] 4.2 GREEN: implement due-policy state, recency window, deterministic jitter/deadlines, and pure eligibility selection without network I/O, then verify task 4.1 passes.
- [x] 4.3 RED: add `crates/instagram-archive/tests/re_resolution_jobs.rs::request_never_starts_when_any_run_or_provider_budget_guard_is_exhausted`; table-drive zero/exhausted item, request, byte, duration, concurrency, and endpoint allowances and confirm a recording resolver sees the unexpected call or missing exact skip class.
- [x] 4.4 GREEN: implement short transactional claim/recheck and capacity reservation for every finite run/provider guard, with no connection held across HTTP, then verify task 4.3 and that all observed counters remain within limits.
- [x] 4.5 RED: add `crates/instagram-archive/tests/re_resolution_jobs.rs::unchanged_refresh_does_not_publish_a_duplicate_update`; confirm equal normalized content currently produces incorrect run/event accounting.
- [x] 4.6 GREEN: route accepted work through existing public-resolution evidence and publishing semantics, distinguish unchanged from updated content, and verify task 4.5 passes.
- [x] 4.7 RED: add `crates/instagram-archive/tests/re_resolution_jobs.rs::deletion_between_selection_and_claim_prevents_request_and_resurrection`; delete a selected candidate before admission and confirm the zero-call/zero-revision/zero-event assertion fails.
- [x] 4.8 GREEN: revalidate owner, local-removal state, policy, recency, and budgets immediately before I/O; preserve prior projections on timeout/malformed/unavailable outcomes, then verify task 4.7 and the full re-resolution target pass.

## 5. Parser-Version Data Export Reprocessing Tooling

- [x] 5.1 RED: add `crates/instagram-archive/tests/data_export_reprocessing.rs::reprocessing_refuses_tampered_receipts_and_unsupported_parser_versions_before_mutation`; confirm the expected integrity/parser refusals and zero-effects assertion are unavailable.
- [x] 5.2 GREEN: split verified receipt access from parsing and add an exact `(detected_layout, parser_id)` registry without fallback, then verify task 5.1 using synthetic/redacted archives only.
- [x] 5.3 RED: add `crates/instagram-archive/tests/data_export_reprocessing.rs::migration_dry_run_matches_apply_report_for_unchanged_state`; assert equal ordered classifications, counts, warnings, conflicts, completeness evidence, prospective/applied digests, and plan fingerprint apart from operation identity/timestamps, and confirm it fails because no shared plan exists.
- [x] 5.4 GREEN: implement the pure deterministic `ReprocessPlan`, canonical plan/state fingerprints, and report rendering shared by dry-run and apply, then verify task 5.3 passes.
- [x] 5.5 RED: add `crates/instagram-archive/tests/data_export_reprocessing.rs::dry_run_does_not_change_database_blob_outbox_or_checkpoint_state`; include normalized, unknown, warning, and conflicting records and confirm the zero-durable-mutation contract is absent.
- [x] 5.6 GREEN: keep dry-run entirely read-only and content-safe, returning no raw bytes/bodies/notes/credentials/private paths, then verify task 5.5 passes.
- [x] 5.7 RED: add `crates/instagram-archive/tests/data_export_reprocessing.rs::apply_resumes_after_committed_checkpoint_and_completed_replay_adds_nothing`; inject interruption after one committed item and confirm resumed/fresh reports or row/fact counts differ.
- [x] 5.8 GREEN: implement bounded deterministic apply chunks, atomic checkpoint/projection/outbox commits, precondition validation, resume, and completed replay, then verify task 5.7 passes.
- [x] 5.9 RED: add `crates/instagram-archive/tests/data_export_reprocessing.rs::parser_omission_never_deletes_existing_capture_source_media_or_prior_evidence`; confirm a deliberately omitted synthetic category is not yet reported with retained state.
- [x] 5.10 GREEN: preserve prior projections/raw evidence on parser omission and keep every reprocessed record at `data_export`/`export_observation`, then verify task 5.9 and the full Data Export targets pass.
- [x] 5.11 RED: add `services/instagram-archive/tests/reprocess_export.rs::process_contract_separates_json_stdout_diagnostics_and_exit_codes`; exercise dry-run/apply, invalid grammar, bad configuration, integrity failure, and broken stdout and confirm the one-document stdout/status contract is absent.
- [x] 5.12 GREEN: extend the existing dependency-free process grammar with explicit `reprocess-export dry-run|apply`, closed arguments, newline-terminated canonical JSON stdout, stderr-only diagnostics, and documented `0/1/2/78` exits, then verify task 5.11 passes without adding an HTTP mutation endpoint or dependency.

## 6. Configuration, Telemetry, Documentation, and Full Gate

- [x] 6.1 RED: extend `crates/instagram-archive/tests/config.rs` with `item9_workers_require_disabled_or_finite_nonzero_budgets`; confirm enabled media/re-resolution/blob/reprocessing configuration currently accepts a zero, missing, or unbounded guard.
- [x] 6.2 GREEN: add strict disabled-by-default item-9 configuration with finite ceilings and cross-field validation, then verify task 6.1 and all configuration tests pass.
- [x] 6.3 RED: extend `crates/instagram-archive/tests/telemetry.rs` with `lifecycle_metrics_cover_bounded_outcomes_without_sensitive_labels`; confirm media/deletion/blob/re-resolution/reprocessing metrics are absent or expose a prohibited high-cardinality/content label.
- [x] 6.4 GREEN: add bounded operation/outcome telemetry and durations for policy admission, deletion phases, pending blob work, re-resolution skips/results, and dry-run/apply/resume, then verify task 6.3 passes without owner/source IDs, usernames, full URLs, captions, notes, credentials, raw bodies, or paths as labels.
- [x] 6.5 Update `README.md`, `DEVELOPMENT.md`, `docs/{ARCHITECTURE,CAPABILITY_MATRIX,DATA_MODEL,DOMAIN,INTERFACES,REQUIREMENTS,TESTING,THREAT_MODEL}.md`, and mark implementation-plan item 9 complete; this cannot start from a failing behavior test because it is documentation, so verify `rg` covers media/privacy/re-resolution/reprocessing, producer/consumer evidence boundaries, rollout/rollback, retention, legacy-import exclusion, and real-export limitations without any database migration or later-major claim.
- [x] 6.6 Run targeted crate/service tests and then the exact documented full gate, placing every compiler-backed Cargo command behind `build-gate`: `git diff --check`, `openspec validate --all --strict`, `openspec validate --archived`, `build-gate -- cargo fetch --locked`, `build-gate -- cargo deny --locked check`, `cargo fmt --all -- --check`, `build-gate -- cargo clippy --workspace --all-targets --locked -- -D warnings`, `build-gate -- cargo build --workspace --locked`, `build-gate -- cargo test --workspace --locked`, `build-gate -- cargo test --workspace --locked --doc`, and `build-gate -- cargo build --workspace --locked --release`; record the exact observed results.
- [x] 6.7 Review the final diff for scope, authority/provenance, privacy completeness, shared-reference safety, no credential/content logging, no hidden/private API, no legacy import, no migration/tooling/version drift, stale generated artifacts, and incomplete call sites; verify `git status --short` contains only intended item-9/OpenSpec changes.
