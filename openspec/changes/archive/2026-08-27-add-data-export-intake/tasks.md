## 1. Approved dependencies and safe fixtures

- [x] 1.1 After explicit repository-owner approval, add workspace dependency `zip = 8.6.0` with default features disabled and only `deflate`, add a pinned `proptest` dev dependency for the untrusted parser, enable only the required Tokio filesystem/I/O features, and verify `cargo metadata --locked --no-deps` plus `cargo deny --locked check` succeed. No RED applies: manifest and lockfile changes are configuration required for the behavior tests to compile.
- [x] 1.2 Add synthetic/redacted Instagram saved-post fixture JSON and deterministic ZIP builders under `crates/instagram-archive/tests/fixtures/data_export/` and `crates/instagram-archive/src/test_support.rs`; verify fixture inspection finds no credential, personal username/note, live owner export, media bytes, or native-Saved assertion. No RED applies: fixtures provide inputs to the following failing behavior tests.

## 2. First-version persistence contract

- [x] 2.1 RED: extend `crates/instagram-archive/tests/schema.rs` with `fresh_schema_has_owner_scoped_data_export_state_machine`; run `build-gate -- cargo test --locked -p ratatoskr-instagram-archive --test schema fresh_schema_has_owner_scoped_data_export_state_machine` and confirm the assertion fails because transition/export-record/report relations, owner/digest uniqueness, or the closed `received | inspected | parsed | reconciled | failed` state constraint is absent—not because the test fails to compile.
- [x] 2.2 GREEN: edit `schema.sql` in place to replace the bootstrap export placeholders with owner-scoped snapshots/runs, ordered transitions, staged export records, and completeness reports; update exact schema inventory assertions and re-run task 2.1 green without adding a migration file or migration tooling.

## 3. Authenticated streaming immutable receipt

- [x] 3.1 RED: add `data_export_configuration_is_disabled_strict_and_secret_free` to `crates/instagram-archive/tests/config.rs`; run `build-gate -- cargo test --locked -p ratatoskr-instagram-archive --test config data_export_configuration_is_disabled_strict_and_secret_free` and confirm valid Data Export keys are currently rejected or the secret token is rendered, while an enabled incomplete configuration is not refused as required.
- [x] 3.2 GREEN: implement strict redacted `DataExportConfig` and limit validation in `crates/instagram-archive/src/config.rs`, then re-run task 3.1 green with the lane disabled by default and enabled only with complete private-root/token/limit configuration.
- [x] 3.3 RED: add `unknown_export_credential_is_refused_without_polling_body` and `authenticated_upload_streams_exact_blob_before_inspection` to `services/instagram-archive/tests/data_export_api.rs`; run `build-gate -- cargo test --locked -p ratatoskr-instagram-archive-service --test data_export_api authenticated_upload` and confirm the existing router returns the wrong status/shape and creates no exact verifiable `received` receipt—not a compile failure.
- [x] 3.4 GREEN: implement bearer-to-owner authentication, `POST /v1/data-exports`, stream-to-create-new staging with SHA-256/byte cap, verified no-overwrite content-addressed publication, owner/digest receipt persistence, typed secret-free responses, and `GET /v1/data-exports/{run_id}`; re-run both task 3.3 tests green and verify an unauthenticated body stream is never polled.
- [x] 3.5 RED: add `receipt_replay_is_owner_scoped_and_overgrowth_leaves_no_object` and `concurrent_existing_blob_is_verified_before_receipt` to `services/instagram-archive/tests/data_export_api.rs`; run each through `build-gate -- cargo test --locked -p ratatoskr-instagram-archive-service --test data_export_api <test-name>` and confirm same-owner retry, cross-owner isolation, over-limit cleanup, or corrupt-winner refusal fails its assertion.
- [x] 3.6 GREEN: add receipt replay/concurrency verification and bounded cleanup without exposing paths/digests in logs; re-run task 3.5 green and verify staged files are absent after every refused upload.

## 4. Hostile ZIP inspector and bounded entry reader

- [x] 4.1 RED: add `zip_slip_and_ambiguous_paths_are_refused_without_writes` and `duplicate_normalized_entries_are_refused_before_parser` to `crates/instagram-archive/tests/data_export.rs`; run each with `build-gate -- cargo test --locked -p ratatoskr-instagram-archive --test data_export <test-name>` and confirm the received run does not reach the precise typed failed state/history or a malicious name is accepted.
- [x] 4.2 GREEN: implement metadata-first ZIP inventory validation in `crates/instagram-archive/src/data_export/archive.rs` for absolute/traversal/backslash/NUL/depth/type/duplicate rules, wire the `received -> inspected | failed` compare-and-swap transition, and re-run task 4.1 green while asserting no archive entry is materialized on disk.
- [x] 4.3 RED: add hostile cases `entry_count_limit_is_exact`, `declared_and_actual_decompressed_limits_are_exact`, and `compression_ratio_limit_is_exact` to `crates/instagram-archive/tests/data_export.rs`; run the hostile target through `build-gate -- cargo test --locked -p ratatoskr-instagram-archive --test data_export hostile_` and confirm at least one excessive archive reaches inspection/parsing or reports the wrong violated limit.
- [x] 4.4 GREEN: enforce checked cumulative entry/compressed/declared-decompressed totals, ratio and actual emitted-byte counters before JSON parsing, then re-run the complete hostile target green with zero normalized rows/outbox facts for every refusal.
- [x] 4.5 RED: add the proptest `arbitrary_archive_bytes_never_panic_or_escape_limits` to `crates/instagram-archive/tests/data_export.rs`; run `build-gate -- cargo test --locked -p ratatoskr-instagram-archive --test data_export arbitrary_archive_bytes_never_panic_or_escape_limits` and confirm a minimized malformed input currently violates the total typed-result invariant rather than failing to compile.
- [x] 4.6 GREEN: close malformed-header, integer-overflow, truncated-stream, encrypted-entry, and unsupported-entry error paths, then re-run task 4.5 and the full hostile archive suite green; if local nightly and `cargo-fuzz` are present, also run a bounded 60-second ZIP-inspector smoke and record its observed result without making tool absence a pass claim. Observed: the full 16-test Data Export target passed and the 60-second libFuzzer smoke completed 1,559,175 runs with no crash or panic.

## 5. Versioned deterministic saved-post parser

- [x] 5.1 RED: add `supported_fixture_parser_is_deterministic_across_entry_and_json_order` to `crates/instagram-archive/tests/data_export.rs`; run `build-gate -- cargo test --locked -p ratatoskr-instagram-archive --test data_export supported_fixture_parser_is_deterministic_across_entry_and_json_order` and confirm equivalent fixtures do not reach equal ordered records/categories/raw digests/parser id with `data_export`/`export_observation` provenance.
- [x] 5.2 GREEN: implement the parser registry and first exact `your_instagram_activity/saved/saved_posts.json` grammar in bounded modules under `crates/instagram-archive/src/data_export/`, persist sorted staged output and the `inspected -> parsed` transition, then re-run task 5.1 green without treating export observation time as capture or publication time.
- [x] 5.3 RED: add `unknown_sections_and_records_are_retained_with_warning` and `unsupported_layout_fails_without_guessed_output` to `crates/instagram-archive/tests/data_export.rs`; run each via the targeted gated command and confirm unknown evidence is discarded, guessed, or missing its raw archive-entry reference/warning.
- [x] 5.4 GREEN: persist unknown entry/record evidence by archive BlobRef plus normalized entry identity and bounded digest/metadata, record typed warnings, fail unsupported layouts without projections, and re-run task 5.3 green with no native-Saved or unverified parser claim.
- [x] 5.5 RED: add `referenced_media_remains_metadata_only` to `crates/instagram-archive/tests/data_export.rs`; run its targeted gated test and confirm a fixture media path/URL is missing its explicit not-archived status or is incorrectly represented as a stored media BlobRef.
- [x] 5.6 GREEN: normalize only validated media references and completeness warnings, never fetch or expand media bytes, then re-run task 5.5 green and verify the only byte BlobRef belongs to the uploaded archive/raw JSON evidence.

## 6. Idempotent reconciliation, SocialSource facts, and completeness

- [x] 6.1 RED: add `reconciliation_replay_is_idempotent_and_export_absence_is_non_destructive` to `crates/instagram-archive/tests/data_export.rs`; run its targeted gated test and confirm replay duplicates a projection/raw record/outbox fact/report or an absent capture receives a tombstone, unavailable status, or removal fact.
- [x] 6.2 GREEN: implement owner-scoped stable-identity reconciliation and the `parsed -> reconciled` transaction with idempotent normalized revisions and SocialSource captured/updated facts, then re-run task 6.1 green without overwriting stronger official/capture provenance.
- [x] 6.3 REGRESSION: add `ambiguous_identity_stays_conflicted_and_stronger_provenance_survives` to `crates/instagram-archive/tests/data_export.rs`; this shared reconciliation branch was already green from task 6.2 when the focused assertion was added, so no independent RED is claimed. Run its targeted gated test and confirm ambiguity remains staged without a projection and stronger authority survives.
- [x] 6.4 GREEN: persist deterministic conflicts and apply the fixed authority precedence, then re-run task 6.3 green with every ambiguous record still separately recoverable.
- [x] 6.5 RED/REGRESSION: add `completeness_report_lists_exact_overlap_differences_and_non_comparable_captures` to `crates/instagram-archive/tests/data_export.rs` and `other_owner_cannot_read_import_report` to `services/instagram-archive/tests/data_export_api.rs`. The completeness math test was observed RED before implementation. The owner-status assertion reused the owner-scoped status branch already implemented and proved in task 3.4, so it passed immediately and no separate RED is claimed. Re-run both targeted tests through the gate and confirm exact `2 matched, 1 export-only, 1 capture-only, 1 non-comparable` math, the authority disclaimer, and owner isolation.
- [x] 6.6 GREEN: compute, validate, persist, and serialize sorted set-based report lists/counts/categories/warnings in the reconciliation transaction; return them only from the owner-scoped no-store status endpoint and re-run both task 6.5 tests green.
- [x] 6.7 RED: add `successful_import_records_exact_durable_transition_history` and `stage_failure_is_terminal_and_retains_archive` to `crates/instagram-archive/tests/data_export.rs`; run each targeted gated test and confirm the transition sequence is not exactly `received -> inspected -> parsed -> reconciled`, a failed run advances again, or its archive disappears.
- [x] 6.8 GREEN: finish bounded polling-worker startup/shutdown and compare-and-swap stage orchestration, then re-run task 6.7 plus the complete Data Export integration target green under PostgreSQL.

## 7. Capability and telemetry truth

- [x] 7.1 RED: add `data_export_is_supported_after_item_eight` to `crates/instagram-archive/src/capability.rs`; run `build-gate -- cargo test --locked -p ratatoskr-instagram-archive data_export_is_supported_after_item_eight` and confirm `DataExport` still reports `Planned` while retaining its `export_observation` ceiling.
- [x] 7.2 GREEN: mark only the completed Data Export lane supported and re-run task 7.1 green; keep Legacy Import planned and native Saved synchronization not supported.
- [x] 7.3 RED: add `data_export_metrics_use_only_closed_non_sensitive_labels` to `crates/instagram-archive/tests/telemetry.rs`; run its targeted gated test and confirm receipt/stage/failure/category observations are missing or can expose a token, URL, username, digest, archive path, warning text, or gap identifier as a metric label.
- [x] 7.4 GREEN: add closed-label receipt, stage, duration, warning/category-count, and gap-count telemetry plus redacted logs keyed only by non-sensitive run/owner identifiers where allowed; re-run task 7.3 green.

## 8. Documentation and complete gate

- [x] 8.1 Update `README.md`, `DEVELOPMENT.md`, `docs/ARCHITECTURE.md`, `docs/DATA_MODEL.md`, `docs/INTERFACES.md`, `docs/TESTING.md`, `docs/CAPABILITY_MATRIX.md`, `docs/THREAT_MODEL.md`, and crate-level docs with the exact route/configuration, limits, parser path/id, raw retention, state machine, report math, telemetry, disabled-default rollout, and synthetic-fixture/real-export evidence gap; verify no artifact claims native Saved synchronization, media backup, deletion from absence, or current real-export compatibility. No RED applies: documentation records behavior proved above.
- [x] 8.2 Run `cargo fmt --all`, `git diff --check`, the 850-line Rust file ratchet, `openspec validate --all --strict`, and `openspec validate --archived`; verify every command exits zero and no new Rust file exceeds the repository limit.
- [x] 8.3 Start the repository's isolated PostgreSQL 17 and NATS fixtures, export `INSTAGRAM_ARCHIVE_TEST_DATABASE_URL` and `INSTAGRAM_ARCHIVE_TEST_NATS_URL`, then run the complete `DEVELOPMENT.md` Rust gate through one machine slot as `build-gate -- sh -c '<documented cargo command list>'`; verify fetch/deny/fmt/clippy/debug build/workspace tests/doc tests/release build all exit zero with no skipped database or broker suite.
- [x] 8.4 Inspect `git diff origin/main...HEAD`, dependency/license/advisory output, tracked fixtures, generated artifacts, and `git status`; tick only observed tasks and leave the change ready for the separate OpenSpec archive workflow and the requested commit/main integration/push/worktree cleanup sequence. No RED applies: this is final evidence and delivery preparation after all behavior is green.
