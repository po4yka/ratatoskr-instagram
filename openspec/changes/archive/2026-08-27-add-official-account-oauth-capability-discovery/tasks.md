Conventions: every compiler-backed command runs through `build-gate`; every Cargo command carries `--locked`; database tests require `docker compose up -d --wait`. Each red task leaves the named test compiling and failing on the stated behavior assertion, never on a missing symbol or typo.

## 1. Provider contract and dependencies

- [x] 1.1 Re-verify the current Instagram API with Instagram Login authorization, token exchange/refresh, account identity, permission inspection, optional revoke, scope names, Graph version support, and usage headers against Meta's official documentation; record the selected version/endpoints in the adapter constants and source comments. Cannot start from a failing test: this is retrieval of the external contract that the provider fixture and implementation must encode. Verify the selected read scope is exactly `instagram_business_basic` and own-media/provider writes are absent.
- [x] 1.2 After explicit dependency approval, pin the fleet-pattern `aes-gcm`, `base64`, `rand`, and `reqwest` Rustls/JSON dependencies exactly, reference only the crates that use them, resolve the candidate lockfile once, inspect new packages and upstream source/build scripts, then verify `cargo metadata --locked --format-version 1`, `cargo deny --locked check bans advisories licenses sources`, and `cargo fetch --locked` succeed. Cannot start from a failing test: manifests and lockfiles are build configuration.

## 2. Current schema contract

- [x] 2.1 Extend `crates/instagram-archive/tests/schema.rs` with `official_oauth_tables_match_the_current_schema`, `credentials_are_unique_per_account`, and `usage_rows_carry_no_request_payload_columns`; assert the D4 tables, columns, closed checks, foreign keys, and uniqueness. Confirm under `build-gate -- cargo test --workspace --locked --test schema official_oauth` that the table-count assertion fails because the tables do not exist.
- [x] 2.2 Edit `schema.sql` in place—no migration or migration tooling—to add/adjust credentials, OAuth flows, permission observations, account capabilities, credential audit, provider usage, and account connection states exactly as D4/D8 require; verify the new tests and every existing schema test pass with `build-gate -- cargo test --workspace --locked --test schema`.

## 3. OAuth configuration and redaction

- [x] 3.1 Add compile-ready `OAuthConfig`/keyring fields and parsing stubs, then add `crates/instagram-archive/tests/config.rs` tests `oauth_disabled_accepts_missing_secrets`, `oauth_enabled_requires_complete_bounded_configuration`, `oauth_keyring_rejects_invalid_or_missing_current_version_without_echo`, `oauth_effective_config_omits_all_secret_fields`, and `production_provider_hosts_cannot_be_overridden`; confirm `build-gate -- cargo test --workspace --locked --test config oauth_` fails because enabled invalid configuration is still accepted and secrets serialize.
- [x] 3.2 Implement strict all-or-none OAuth configuration, exact Graph-version/redirect/keyring validation, redacted secret wrappers, finite timeouts/retry/call budgets, and test-only loopback provider injection; verify the OAuth config tests plus existing config/boot/admin tests pass with `build-gate -- cargo test --workspace --locked --test config && build-gate -- cargo test --workspace --locked --test boot && build-gate -- cargo test --workspace --locked --test admin`.

## 4. Authenticated credential envelopes

- [x] 4.1 Create `crates/instagram-archive/src/credentials/crypto.rs` with the final typed API and `Unsupported` seal/open stubs, then add unit tests `token_encryption_round_trip`, `equal_plaintexts_use_distinct_nonces`, `wrong_key_version_is_refused`, `tampered_envelope_is_refused`, `account_substitution_is_refused`, and `token_kind_substitution_is_refused`; confirm `build-gate -- cargo test --workspace --locked credentials::crypto` fails on the `Unsupported` result assertions.
- [x] 4.2 Implement the versioned AES-256-GCM envelope with OS randomness, account/flow and token-kind associated data, keyring selection, authenticated open, and redacted errors; verify all crypto tests pass with `build-gate -- cargo test --workspace --locked credentials::crypto`.

## 5. Capability reconciliation

- [x] 5.1 Add compile-ready observation/capability/reason types and a placeholder total reconciler in `capability_reconciliation.rs`, then add `crates/instagram-archive/tests/account_capabilities.rs` tests `business_basic_grant_exposes_only_read_capabilities`, `creator_basic_grant_exposes_only_read_capabilities`, `declined_expired_absent_and_unknown_permissions_are_unavailable`, `personal_and_unknown_accounts_grant_nothing`, `native_saved_is_always_not_supported`, and `own_account_sync_lane_remains_planned`; confirm the focused test fails because the placeholder matrix does not match the expected closed rows.
- [x] 5.2 Implement the pure closed reconciliation rules from D5 without changing `AcquisitionMode::OwnAccountSync`, and verify `build-gate -- cargo test --workspace --locked --test account_capabilities` plus `build-gate -- cargo test --workspace --locked --test capability` pass.
- [x] 5.3 Add DB-backed tests `reconciliation_replaces_the_whole_prior_generation` and `two_accounts_never_share_observations` to `tests/account_capabilities.rs` against persistence stubs; confirm the first fails because stale available rows remain and the second fails because no account-scoped projection is loaded.
- [x] 5.4 Implement transactional permission-evidence and total capability-generation replacement with raw-record linkage and account-scoped reads; verify both DB scenarios and the pure matrix suite pass with `build-gate -- cargo test --workspace --locked --test account_capabilities`.

## 6. Durable provider call budgets

- [x] 6.1 Add a compile-ready `provider_budget` API whose admission stub refuses every attempt, then add `crates/instagram-archive/tests/provider_budget.rs` tests `admission_reserves_started_usage_before_transport`, `completed_attempt_records_redacted_outcome_and_bounded_usage_percentages`, `failed_retry_consumes_a_second_ordinal`, `exhausted_budget_does_not_invoke_transport`, and `crash_left_started_attempt_remains_consumed`; confirm the first test fails because no durable reservation row exists.
- [x] 6.2 Implement per-operation finite admission, committed pre-I/O reservation, terminal update, unique ordinal accounting, bounded documented Meta usage percentages, and typed exhaustion; verify `build-gate -- cargo test --workspace --locked --test provider_budget` passes.

## 7. Official provider and relay adapters

- [x] 7.1 Add the final `InstagramProvider` and `OAuthCodeRelay` port surfaces with unsupported stubs, checked-in synthetic fixtures under `crates/instagram-archive/tests/fixtures/meta/`, and `tests/provider.rs` cases `exchange_request_matches_documented_instagram_login_contract`, `account_and_permission_fixtures_parse_strictly`, `bearer_token_never_appears_in_url_or_diagnostics`, `oversized_or_unknown_provider_response_is_refused`, `auth_validation_rate_limit_server_and_network_failures_classify_typed`, `discovery_retries_only_transient_failures_within_budget`, and `oauth_code_exchange_is_never_retried`; confirm the request/assertion tests fail because the adapter stubs issue no calls.
- [x] 7.2 Implement the Reqwest/Rustls production provider, strict capped response parsers, documented refresh/revoke behavior, bounded transient-only discovery retry, and Platform relay client; add fake provider/relay implementations under `test-support`, then verify `build-gate -- cargo test --workspace --locked --test provider` passes with no live Meta or Platform dependency.

## 8. Owner-bound OAuth begin

- [x] 8.1 Add OAuth begin stubs and `crates/instagram-archive/tests/oauth.rs` tests `begin_returns_owner_bound_single_read_scope_authorize_url`, `begin_stores_state_hash_not_raw_state`, `begin_encrypts_pkce_verifier_when_supported`, and `begin_flow_expires_at_the_configured_deadline`; confirm `build-gate -- cargo test --workspace --locked --test oauth begin_` fails because the stub returns no authorization URL/flow row.
- [x] 8.2 Implement unpredictable state, provider-correct PKCE, owner and redirect binding, hash/encrypted flow persistence, exact `instagram_business_basic` authorization URL construction, and injected clock/random seams; verify all begin tests pass with `build-gate -- cargo test --workspace --locked --test oauth begin_`.

## 9. OAuth completion and encrypted connection persistence

- [x] 9.1 Extend `tests/oauth.rs` against completion stubs with `unknown_expired_owner_mismatched_redirect_mismatched_and_replayed_state_persist_nothing`, `successful_completion_claims_relay_once_and_consumes_flow`, `exchange_failure_consumes_flow_without_connection`, `successful_completion_stores_no_plaintext_and_links_raw_discovery_evidence`, and `successful_completion_accounts_exchange_identity_and_permission_calls`; confirm the success test fails because no connected account/credential/capability/audit/usage state is written.
- [x] 9.2 Implement atomic flow claim, one-time relay exchange, provider code exchange, identity/permission discovery, authenticated token sealing, raw evidence preservation, account upsert, full capability reconciliation, and authorization audit transaction; verify the whole completion matrix passes with `build-gate -- cargo test --workspace --locked --test oauth completion_` and the raw DB assertions find none of the fixture code/state/verifier/tokens.

## 10. Refresh and downgrade handling

- [x] 10.1 Add `crates/instagram-archive/tests/account_lifecycle.rs` cases `refresh_replaces_encrypted_material_expiry_permissions_and_capabilities_together`, `refresh_permission_downgrade_removes_stale_capability`, `refresh_auth_failure_marks_reauthorization_and_disables_all_capabilities`, `refresh_transient_failure_preserves_prior_connection`, and `refresh_without_provider_supported_strategy_is_typed`; confirm the first fails against refresh stubs because stored ciphertext and projections remain unchanged.
- [x] 10.2 Implement provider-profile refresh strategy, budgeted rediscovery, atomic credential/projection/audit replacement, authentication downgrade, and transient preservation semantics; verify `build-gate -- cargo test --workspace --locked --test account_lifecycle refresh_` passes.

## 11. Complete local revocation

- [x] 11.1 Add `account_lifecycle.rs` tests `revoke_scrubs_every_credential_and_live_owner_flow_marks_account_and_disables_capabilities`, `provider_revoke_failure_still_scrubs_locally`, `unsupported_provider_revoke_still_scrubs_locally`, `revoke_audit_and_usage_are_redacted`, and `startup_scrubs_account_stranded_in_revoking`; inspect every owned byte/text/json column and confirm `build-gate -- cargo test --workspace --locked --test account_lifecycle revoke_` fails because credential/flow material remains.
- [x] 11.2 Implement revoking transition, one budgeted documented provider attempt when supported, unconditional local scrub transaction, total revoked capability projection, audit result, idempotent repeated revoke, and startup scrub of stranded revocations; verify every revoke/startup scenario passes with `build-gate -- cargo test --workspace --locked --test account_lifecycle`.

## 12. Loopback service API and telemetry

- [x] 12.1 Add compile-ready route handlers returning a typed unavailable result, then add `services/instagram-archive/tests/account_api.rs` tests `begin_complete_refresh_capabilities_and_revoke_routes_are_loopback_commands`, `complete_accepts_relay_id_and_never_authorization_code`, `owner_mismatch_is_refused`, `disabled_oauth_routes_are_unavailable`, and `account_responses_never_contain_credentials`; confirm `build-gate -- cargo test --workspace --locked --test account_api` fails because enabled routes do not execute the lifecycle.
- [x] 12.2 Wire the lifecycle and fake/production dependencies into the product router/runtime, preserving the existing capture routes and owner checks; verify `build-gate -- cargo test --workspace --locked --test account_api` plus `build-gate -- cargo test --workspace --locked --test capture_api` pass.
- [x] 12.3 Add telemetry capture tests `oauth_failures_and_usage_metrics_have_bounded_labels` and `synthetic_secrets_never_appear_in_logs_metrics_audit_usage_or_http_errors` across every failure path; confirm the focused tests fail because required counters/classes are absent or a sentinel appears.
- [x] 12.4 Implement bounded lifecycle/usage metrics and redacted structured events without usernames, captions, full URLs, provider bodies, or credential material; verify the new telemetry/redaction tests and existing telemetry suite pass with `build-gate -- cargo test --workspace --locked telemetry`.

## 13. Documentation and final lifecycle

- [x] 13.1 Update `README.md`, `DEVELOPMENT.md`, `docs/CAPABILITY_MATRIX.md`, `docs/INTERFACES.md`, `docs/DATA_MODEL.md`, `docs/THREAT_MODEL.md`, and `.env.example` if present to describe the implemented official account lane, read-only requested scope, encryption/keyring and retention, finite API budgets, capability reasons, revoke semantics, disabled-by-default rollout, item 7 boundary, and the separate Platform callback/provider/grant prerequisite. Cannot start from a failing test: these are operator and architecture artifacts. Verify documented tables/routes/config/metrics match code and `schema.sql` by review and `git diff --check`.
- [x] 13.2 Run the complete local gate against PostgreSQL 17: `cargo fetch --locked`, `cargo deny --locked check`, `cargo fmt --all -- --check`, then every compiler-backed Clippy/build/test/doc/release command from `DEVELOPMENT.md` through one top-level `build-gate --`; also run the Rust file-size ratchet, CI/docs command-list drift guard, `git diff --check`, and `openspec validate add-official-account-oauth-capability-discovery --strict`. All commands must exit 0 before checking this task.
- [x] 13.3 Sync both delta specs into the main specs, verify the merged requirements exactly, archive the completed change, and run `openspec validate --all --strict` plus `openspec validate --archived`; inspect the final diff/status for unrelated edits, secret fixtures, and scope creep. Cannot start from a failing behavior test: this is the required OpenSpec closeout gate.
